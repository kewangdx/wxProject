<!--components/fance/inde.wxml-->
<view>
    <view class="title">{{fence.title}}</view>
    <view class="row-container">
        <block wx:for="{{fence.cells}}" wx:key="{{index}}">
            <s-cell data-y="{{index}}" class="cell" cell="{{item}}" />
        </block>
        <view class="hr"></view>
    </view>
</view>